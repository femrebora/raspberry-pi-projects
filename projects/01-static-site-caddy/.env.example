# Domain Caddy should serve.
# Examples:
#   :80                       (local HTTP only, no TLS — good for testing)
#   pi5.example.com           (public HTTPS via Let's Encrypt — needs ports 80+443 open)
#   pi5.example.com:8443      (custom port)
SITE_DOMAIN=:80
