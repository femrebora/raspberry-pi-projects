# Caps Docker JSON log size; works alongside the per-service `logging:` blocks.
/var/lib/docker/containers/*/*.log {
    rotate 5
    daily
    compress
    missingok
    delaycompress
    copytruncate
    notifempty
    size 10M
}
