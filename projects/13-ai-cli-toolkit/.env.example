# This project doesn't run as a service — it installs CLIs. There's no .env to
# load. Instead, after running install.sh, edit ~/.ai_keys (created from
# ~/.aiclirc) and put every API key there.
#
# See README.md for the list of providers and where to sign up.
